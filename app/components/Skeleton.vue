<template>
    <div class="space-y-1">
        <div class="flex gap-2 items-center py-2 rounded-lg">
            <USkeleton
                class="h-10 w-10 flex-shrink-0"
                :ui="{ rounded: 'rounded-full' }"
            />
            <USkeleton class="h-10 w-full" />
        </div>
        <div class="flex gap-2 items-center py-2 rounded-lg">
            <USkeleton
                class="h-10 w-10 flex-shrink-0"
                :ui="{ rounded: 'rounded-full' }"
            />
            <USkeleton class="h-10 w-full" />
        </div>
        <div class="flex gap-2 items-center py-2 rounded-lg">
            <USkeleton
                class="h-10 w-10 flex-shrink-0"
                :ui="{ rounded: 'rounded-full' }"
            />
            <USkeleton class="h-10 w-full" />
        </div>
    </div>
</template>
